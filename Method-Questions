Q.1)What is the return type of method Below?
    Public int Calculate(int a,int b)
    {
	return a+b;
    } 
    a)int 
    b)void
    c)double
    d)error
Answer-a)int

Q.2)Which of the following method must contain return Statement?
   a)void
   b)Constructor
   c)int
   d)static
Answer-c)int

Q.3)What happen if a method with return type String does not return anything?
   a)The method return empty String.
   b)The method causes a compile-time-error.
   c)The method return null by default.
   d)The method causes ren-time Exception.
Answer-b)The method causes compile-time-error.

Q.4)Which return type allow you to return any object in java?
   a)void
   b)Object
   c)String
   d)Class
Answer-b)Object

Q.5)What is the correct return type for a method that does not return any value?
  a)null
  b)int 
  c)Object
  d)void
Answer-d)void

Q.6)A Method with return type void cannot use the return keyword?
  a)True
  b)false
 Answer-a)True
 Explaination- void does not return with value exa-return a; but 
               we can use only return; keyword with void.

Q.7)Every method must have a return Statement?
   a)True
   b)False
Answer-b)False

Q.8)you can use return; in a method with return type void.
   a)true
   b)false
Answer-b)false.

Q.9)The return type of method determines the type of data it can be 
     send back to the caller.
   a)true
   b)false
Answer-a)true

Q.10What happens when the folloeing mrthod is compiled and run?
  public double getPrice()
  {
  }
  a)it returns 0.0 default
  b)it compiles Succesfully
  c)it causes a compile time error 
  d)it throws a run time exception
Answer-c)it causes a compile time error 

Q.11)identify the error in method below?
  public boolean isEven(int num)
  {
  	if(num%2==0)
	{
		return true;
	}
  }
  a)No error
  b)Method might not return a value
  c)return used incorrectly
  d)Syntax error in if Statement
 Answer-b)Method might not return a value.
 explanation- it throws error Missing return Statement.
